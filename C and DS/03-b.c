#include <stdio.h>

int main()
{
    int var = 3;
    var =4;
    printf("%d", var);
    int var1;
    var1 = var;
    int var2, var3, var4 = 4;
    return 0;
}


/*
once you define its memory or datatype you cannot do that again because it is already defined. but it can used with different assignment in program.
we cannot define same variable with same name.
also assign variable to variable.
also same value to different variable in single line
*/